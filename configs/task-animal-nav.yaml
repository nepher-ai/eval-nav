# Evaluation configuration for task-animal-nav
# 
# This config evaluates the ANYmal B waypoint navigation environment
# using envs-nav scenes for standardized evaluation.

task_name: "Nepher-Animal-WaypointNav-Envs-Play-v0"

# Python module to import for environment registration
task_module: "animalnav"

# Environment-scene combinations to evaluate
# Each entry specifies a nav_env_id and nav_scene to evaluate
env_scenes:
  - nav_env_id: "waypoint-benchmark-v1"
    nav_scene: 0

# Random seeds for reproducibility
seeds:
  - 42

# Number of episodes per scene-seed combination
num_episodes: 2

# Maximum steps per episode (None = use environment default)
max_episode_steps: 900

# Number of parallel environments for evaluation (mandatory)
num_envs: 10

# Scoring version (MVP supports v1 only)
scoring_version: "v1"

# Logging
log_dir: "logs/animal-nav"
enable_logging: true

# Timeout for entire evaluation (seconds, None = no timeout)
timeout_seconds: null

# Policy checkpoint path (optional)
# If "default" or not specified, will look for "best_policy/best_policy.pt" 
# in the task project folder (task-animal-nav)
policy_path: "default"

